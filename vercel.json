{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pip install -r requirements.txt && cd web && npm ci",
  "buildCommand": "cd web && npm run build",
  "crons": [
    {
      "path": "/cron",
      "schedule": "*/10 * * * *"
    }
  ]
}
