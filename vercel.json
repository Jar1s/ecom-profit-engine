{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pip install -r requirements.txt && cd dashboard-web && npm ci",
  "buildCommand": "cd dashboard-web && npm run build",
  "crons": [
    {
      "path": "/cron",
      "schedule": "*/10 * * * *"
    }
  ]
}
